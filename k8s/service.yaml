apiVersion: v1
kind: Service
metadata:
  name: safety-risk-service
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: safety-risk-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082
